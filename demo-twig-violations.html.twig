{# DEMO - DO NOT MERGE.

  Deliberately trips the .twig-side checks in bin/check-twig-safety.sh so
  the "Twig safety check" step in CI fails. Delete this file before merging
  the parent PR. This template is never registered or rendered.
#}

{% sandbox %}
	{% include "malicious.twig" %}
{% endsandbox %}

<div>{{ user_input|spaceless }}</div>

{% apply spaceless %}
	<p>compressed</p>
{% endapply %}
