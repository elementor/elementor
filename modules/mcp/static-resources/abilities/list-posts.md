Lists WordPress posts and pages with pagination support. Use this to discover existing content on the site by `id`, so you can reference posts when creating links or retrieving content. Default returns 10 posts sorted by most recently created. Use `search` to filter by keywords in title or content.
