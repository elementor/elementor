<?php

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

return [
	'widget_type' => 'counter',
	'description' => 'V3 animated counter (starts from `starting_number`, counts up to `ending_number` over `duration` ms when scrolled into view).',
];
