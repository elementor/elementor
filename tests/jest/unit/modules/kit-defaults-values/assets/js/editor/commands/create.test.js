describe( `$e.run( 'kit-elements-defaults/create' )`, () => {
	//
	// let command;
	//
	// beforeAll( async () => {
	// 	// Need to import dynamically since the command extends a global variable which isn't available in regular import.
	// 	command = ( await import( 'elementor/modules/kit-elements-defaults/assets/js/editor/commands' ) ).Create;
	// } );
	//
	// beforeEach( () => {
	// 	global.$e = {
	// 		internal: jest.fn(),
	// 		modules: {
	// 			CommandBase: class {},
	// 		},
	// 	};
	// } );
} );
