// eslint-disable-next-line import/no-extraneous-dependencies
import { createLocation } from '@elementor/locations';

export const { inject: injectIntoInteractionTriggerSlot, Slot: TriggerSlot } = createLocation();

export const { inject: injectIntoInteractionAnimationSlot, Slot: AnimationSlot } = createLocation();

// export const { inject: injectIntoPrimaryAction, Slot: PrimaryActionSlot } = createLocation();
