{
	"name": "test-server",
	"version": "1.0.0",
	"main": "test-server.js",
	"type": "module",
	"scripts": {
		"build": "tsc",
		"cli": "node --enable-source-maps test-server.js cli config=../.test-server.config.json",
		"start": "node --enable-source-maps test-server.js start config=../.test-server.config.json",
		"stop": "node --enable-source-maps test-server.js stop config=../.test-server.config.json",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"description": "",
	"dependencies": {
		"docker-compose": "^0.24.8"
	},
	"devDependencies": {
		"typescript": "^5.5.3"
	}
}
