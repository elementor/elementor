export default class HookAgreement extends Error {
	/**
	 * @param [args={}]
	 * @return {function()} - callback.
	 */
	newAgreement( args = {} ) { // eslint-disable-line no-unused-vars
		elementorModules.forceMethodImplementation();
	}
}
