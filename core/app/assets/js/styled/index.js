import { StyledButton } from 'elementor-app/styled/styled-button';
import { StyledGrid } from 'elementor-app/styled/styled-grid';
import { StyledHeading } from 'elementor-app/styled/styled-heading';

export const Styled = {
	Button: StyledButton,
	Grid: StyledGrid,
	Heading: StyledHeading,
};
