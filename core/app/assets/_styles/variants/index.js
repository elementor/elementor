const Heading = require('./heading.js');
const HeadingDark = require('./heading-dark');
const Text = require('./text.js');
const Button = require('./button.js');

const variants = {
	Heading,
	HeadingDark,
	Text,
	Button,
};

module.exports = variants;
